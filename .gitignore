# Chave secreta do gerador (se criar arquivo local no PC)
admin_secrets.py

# Build (APKs, Flutter, site-packages)
build/

# APK empacotados (muitas vezes >100MB — limite GitHub)
releases/*.apk
releases/*.apk.sha1
# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/

# IDE
.idea/

# Licença local
.auditcalc_*.json
.auditoria_banco.json

# Supabase CLI cache local (não versionar)
supabase/.temp/

# Cursor workspace
.cursor/

# Payloads / testes HTTP locais (podem conter IDs reais do Mercado Pago)
body.json
*-webhook-body.json

# Locks do LibreOffice ao editar documentos na pasta do projeto
.~lock.*

# Docs locais / segredos (não públicos)
códigos github.txt
Audit Calc Chave de Ativação.pdf
credenciais no mercado pago.odt
link para download audit calc.odt
email de envio de chave.odt
senha do gerador de senhas.odt
conversa com chatgpt final da programação.odt
senha gerador de senhas.txt

# Também aceitar nomes com espaços quando o cliente Git exige aspas:
"email de envio de chave.odt"
"senha do gerador de senhas.odt"
"conversa com chatgpt final da programação.odt"
"link para download audit calc.odt"
"credenciais no mercado pago.odt"
audit calc pro link.pdf

# Arquivo grande (>100MB - limite GitHub)
site google.odt
